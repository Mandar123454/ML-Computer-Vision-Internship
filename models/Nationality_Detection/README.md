# Nationality_Detection models

Suggested artifacts:
- nationality_model.h5
- emotion_model.h5 (if separate)

Notes:
- The notebook uses Gradio with placeholder predictions. When you integrate real models, store them here and note their expected input shapes.
- If you rely on feature extractors, include version info and links.