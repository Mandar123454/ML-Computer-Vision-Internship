# Attendance_System models

Suggested artifacts:
- knn_classifier.pkl (trained KNN on FaceNet embeddings)
- facenet_model.h5 (if you fine-tune or freeze a FaceNet variant)

Notes:
- The notebook simulates embeddings; for real use, extract embeddings per student and train KNN, then save here.