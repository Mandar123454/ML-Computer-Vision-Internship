# Animal_Detection models

Suggested artifacts:
- animal_detector.weights.h5 (or TensorFlow SavedModel directory)
- label_map.txt (class names)

Notes:
- The current notebook uses TensorFlow and may optionally pull features from tensorflow-hub.
- Save fine-tuned weights here when available.